int a;
int b;
int x;
int y;

void main() {
    		write("Informe um numero (a): ");
    		read( a );
			write("Informe um numero (b): ");
 			read( b );
			x = y = a > b ? a + b : a * b;
			write("x: ", x);
			write("y: ", y);
}